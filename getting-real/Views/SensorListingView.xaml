<UserControl x:Class="getting_real_4.Views.SensorListingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="10">
        <TextBlock Text="Registrerede sensorer" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        <DataGrid ItemsSource="{Binding Sensors}" Margin="0,38,0,40" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" IsReadOnly="True" />
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" />
                <DataGridTextColumn Header="Sensor Type" Binding="{Binding SensorType}" />
                <DataGridTextColumn Header="Connection" Binding="{Binding ConnectionType}" />
                <DataGridTextColumn Header="Keys" Binding="{Binding Keys}" />
				<DataGridTextColumn Header="Er hjemme?" Binding="{Binding IsHome}" />
				<DataGridTextColumn Header="Battery Replacements" Binding="{Binding BatteryReplacementCount}" IsReadOnly="True" />
                <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Replace Battery" Command="{Binding ReplaceBatteryCommand}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Tiloej fyldemelder" Command="{Binding AddSensorCommand}" HorizontalAlignment="Left"
                Margin="0,0,0,0"
                VerticalAlignment="Bottom" />
    </Grid>
</UserControl>